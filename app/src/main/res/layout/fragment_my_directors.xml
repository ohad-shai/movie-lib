<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:gravity="center"
    android:orientation="vertical"
    tools:context="com.ohadshai.movielib.ui.fragments.MyDirectorsFragment">

    <ProgressBar
        android:id="@+id/progressBar"
        style="?android:attr/progressBarStyleLarge"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:visibility="visible" />

    <android.support.v7.widget.RecyclerView
        android:id="@+id/rvDirectors"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:clipToPadding="false"
        android:paddingBottom="100dp"
        android:scrollbarStyle="outsideOverlay"
        android:scrollbars="vertical"
        android:visibility="gone" />

    <Button
        android:id="@+id/btnDirectorsNotFound"
        android:layout_width="300dp"
        android:layout_height="230dp"
        android:background="@color/colorTransparent"
        android:drawableTop="@mipmap/ic_empty_directors"
        android:text="@string/directors_not_found"
        android:textAllCaps="false"
        android:textColor="#444"
        android:textSize="20sp"
        android:visibility="gone" />

</LinearLayout>